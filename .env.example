# Ragify — environment templates
#
# Local dev:  cp apps/web/.env.example apps/web/.env.local
# DO droplet: cp deploy/do/.env.example deploy/do/.env
# OCI VMs:    cp deploy/oci/.env.example deploy/oci/.env
#
# See docs/DEPLOY.md
